import React, { PropTypes } from 'react';
import styles from '../styles/facebook.scss';

class FaceData extends React.Component { 
	render(){
				return(<h1>The App is ready</h1>);
		};
}

export default FaceData